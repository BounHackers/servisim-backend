{
  "environments": {
    "test": {
      "scripts": {
        "test": "bundle install && bundle exec rake"
      },
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ]
    }
  }
}