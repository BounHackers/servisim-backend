{
  "environments": {
    "test": {
      "scripts": {
        "test": "bundle install && bundle exec rubocop --auto-correct"
      },
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ]
    }
  }
}