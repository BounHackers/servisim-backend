{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "bundle install",
        "test": "bundle exec rubocop --auto-correct"
      },
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ]
    }
  }
}