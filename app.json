{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "gem install rubocop",
        "test": "bundle exec rubocop --auto-correct"
      },
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ]
    }
  }
}