{
  "environments": {
    "test": {
      "scripts": {
        "test": "gem install rubocop && rubocop ."
      },
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ]
    }
  }
}